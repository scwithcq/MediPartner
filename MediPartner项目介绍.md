# "医伴" —— 基于大模型的信任型陪诊服务平台系统设计文档

## 1. 项目背景与产品定位

**1.1 项目背景** 随着人口老龄化加剧及独生子女异地就业常态化，"异地父母就医难"成为社会痛点。同时，本地化灵活就业需求旺盛。本平台旨在利用 AI 大模型技术，搭建一个连接"异地子女（求助者）"与"本地服务者（陪诊师）"的信任型服务平台。

**1.2 产品定位** 这是一个**以微信小程序为核心入口、PC Web 管理后台为运营支撑**的 SaaS 平台，以"医疗陪诊"为核心切入场景，后续具备向"宠物服务、家政服务"等领域横向复用的能力。平台核心价值在于：**通过 AI 辅助决策（RAG）解决信息不对称，通过严谨的资金账户体系解决信任问题。**

------

## 2. 系统功能模块详细描述

系统采用**多角色权限隔离**设计，包含两大用户终端：

- **C 端**：微信小程序（求助者 + 陪诊师共用小程序，根据角色展示不同页面）
- **B 端**：PC 端 Web 管理后台（运营管理员使用）

### 2.1 用户端（求助者/家属）—— 微信小程序

**目标用户**：无法亲自陪同就医的异地子女或行动不便老人的家属。

**核心功能点**：

1. **用户注册与身份认证**：支持微信一键登录（获取 OpenID），绑定手机号后完成注册，默认身份为普通用户。
2. **AI 智能发单助手**：
   - 用户填写症状描述、期望医院城市、陪诊时间。
   - 系统调用后台 RAG 能力，检索当前已生效的知识库文档，为用户推荐 3 个最合适的挂号科室及医院建议，并附带推荐理由与引用来源。
3. **服务者浏览与筛选**：查看平台认证通过的陪诊师列表，支持按好评率、服务次数、服务区域进行排序筛选。
4. **虚拟币充值（模拟）**：提供预设金额选项（如 50、100、200 虚拟币），模拟微信支付回调，完成账户余额充值。
5. **下单与资金冻结**：用户确认需求后下单，系统立即**冻结**用户账户中的对应服务费，订单进入"接单大厅"，等待陪诊师抢单。
6. **订单状态跟踪**：实时查看订单状态（待接单 / 已接单 / 服务中 / 待确认 / 已完成），支持一键联系陪诊师（隐私号拨打）。
7. **服务确认与报告查看**：陪诊服务结束后，用户确认完成。系统支持查看 AI 自动生成的结构化《陪诊服务报告》（包含就诊关键医嘱摘要）。
8. **评价体系**：对完成服务的陪诊师进行星级评价、文本评价，支持追评与回复。好评率基于全部历史评价计算。
9. **申诉与退款**：对存在争议的订单提交申诉工单，上传证据，等待平台仲裁。

### 2.2 陪诊师端（接单者）—— 微信小程序

**目标用户**：具备本地医院陪诊经验、通过平台资质审核的自由职业者。

**核心功能点**：

1. **入驻申请与资质审核**：填写个人身份信息、上传身份证及健康证（或相关承诺书），提交至管理后台进行**多维度资质审核**（身份真实性、无犯罪记录承诺、健康状态）。审核通过后，该用户自动获得陪诊师角色权限，初始信用分为 100 分。
2. **接单大厅**：
   - 查看当前待接单的抢单列表。
   - 系统利用地理位置（GeoHash）算法，基于陪诊师当前微信授权定位，优先展示距离近的订单。
   - 支持一键抢单。采用"先到先得"原则，后端通过乐观锁防止超卖（同一订单被多人抢）。
   - **接单上限控制**：平台对陪诊师设置**最大接单上限（默认 3 单）**。当陪诊师当前处于"已接单"或"服务中"状态的订单达到上限时，抢单按钮自动置灰并提示"当前接单已达上限，请先完成现有服务"。
2. **接单大厅**：
   - 查看当前待接单的抢单列表。
   - 系统利用地理位置（GeoHash）算法，基于陪诊师当前微信授权定位，优先展示距离近的订单。
   - 支持一键抢单。采用"先到先得"原则，后端通过乐观锁防止超卖（同一订单被多人抢）。
3. **我的工作台**：分类展示"待服务"、"服务中"、"已完成"的订单列表。
4. **服务履约与过程留痕**：
   - 到达医院后点击"开始服务"变更状态，系统自动记录 GPS 定位与时间戳。
   - **服务过程留痕机制**：服务期间，陪诊师必须**每隔 30 ~ 60 分钟**通过小程序上传一张带**时间水印与 GPS 定位水印**的现场照片或与被陪诊人的合影。照片上传后将实时同步至用户端，供下单用户查看，确保服务真实可溯。
   - 服务结束后，需拍照上传就诊凭证（如挂号单、检查单照片）方可申请进入结算流程。
   - 若陪诊师未按时上传过程留痕照片，系统将向管理后台触发**实时告警**，并依据信用分规则予以扣分。
5. **信用中心**：
   - 查看个人信用分（初始 100 分）及信用记录明细。
   - **信用分扣减场景**：无故爽约、迟到超过 15 分钟、未按时上传过程留痕照片、被用户投诉且经平台核实等，均会按规则扣减信用分。
   - **信用分等级与权限关联**：信用分低于 80 分时，每日接单上限降至 1 单；低于 60 分时，暂停抢单权限，需参加平台培训并考核通过后方可恢复。
6. **收益查看**：查看累计虚拟币收入及可提现余额（提现申请提交至管理后台审批）。
7. **评价与回复**：查看用户评价，支持对评价进行文字回复。
   - 到达医院后点击"开始服务"变更状态。
   - 服务结束后，需拍照上传现场凭证（如挂号单、检查单照片）方可申请进入结算流程。
5. **收益查看**：查看累计虚拟币收入及可提现余额（提现申请提交至管理后台审批）。
6. **评价与回复**：查看用户评价，支持对评价进行文字回复。

### 2.3 管理后台（运营者）—— PC Web 端

**目标用户**：平台运营管理员（系统超级管理员）。

**核心功能点**：

1. **准入审核中心**：审核陪诊师提交的入驻申请，决定"通过"或"驳回"。通过后，该用户自动获得陪诊师角色权限。
2. **全量订单监控**：查看平台所有进行中及已完成订单的详细流转日志，具备异常订单的人工干预（取消、退款）权限。
3. **资金审计与风控**：
   - 查看所有用户的虚拟币流水明细（充值、消费、收入、提现）。
   - 利用流水表的快照机制，确保每笔资金变动可追溯。
4. **模拟提现审批**：处理陪诊师提交的虚拟币转微信零钱申请（当前版本为管理员后台点击"模拟打款"，状态变更完成）。
5. **申诉工单仲裁**：查看用户提交的申诉工单，调阅订单凭证与双方沟通记录，做出"维持原判 / 部分退款 / 全额退款"仲裁。
6. **知识库管理**：
   - 上传新的 PDF 文档、删除旧文档。
   - 点击"发布知识库"触发异步任务（RabbitMQ）：解析 PDF → 切片 → 生成向量 → 写入向量库。
   - 版本控制：新文档生效后，旧版本自动标记为"已失效"。AI 检索仅查询"已生效"的切片。
7. **管理员操作审计**：所有管理员的人工干预操作（取消订单、强制退款、审核通过/驳回）均记录操作人、时间、订单号、变更前后数据快照（JSON），永久留存备查。

------

## 3. 核心业务闭环流程（数据流向）

### 3.1 主流程

1. **发单阶段**：求助者填写需求 → AI 检索医疗知识库辅助挂号决策 → 用户确认下单 → 系统冻结服务费 → 订单进入接单大厅。
2. **匹配与缔约阶段**：陪诊师抢单（先到先得） → 抢单成功者与订单绑定 → 订单状态变更为"待服务"。
3. **履约阶段**：陪诊师线下陪同 → 线上点击"开始/结束服务"并上传照片凭证 → 系统记录服务轨迹。
4. **结算与 AI 复盘阶段**：求助者确认完成（或超时自动确认） → 后台执行**强一致性事务**（解冻扣款 + 陪诊师入账） → 触发 AI 生成陪诊报告 → 双方互评 → 订单终态。

### 3.2 超时与异常流转规则

表格





| 场景                       | 超时规则    | 系统行为                                                     |
| :------------------------- | :---------- | :----------------------------------------------------------- |
| 发单后无人接单             | X = 24 小时 | 自动取消订单，全额解冻退款至用户                             |
| 陪诊师结束服务后用户未确认 | Y = 48 小时 | 自动确认完成，执行资金划转                                   |
| 服务开始超过未结束         | Z = 12 小时 | 触发异常预警，标记为"异常订单"，后台介入，管理员可调阅双方信息与凭证并询问进程 |

### 3.3 申诉工单流程

1. 用户对已完成或待确认订单发起申诉，填写申诉原因并上传证据（图片/文字）。
2. 系统生成申诉工单，状态为"待处理"，关联原订单。
3. 管理员在后台查看工单，调阅订单全流程日志、双方上传的凭证、资金流水。
4. 管理员做出仲裁：维持原判 / 部分退款 / 全额退款。
5. 仲裁结果通知双方，若涉及退款则执行资金回滚/补偿，工单状态变更为"已结案"。

------

## 4. 平台技术架构描述（无代码层级）

### 4.1 总体架构风格：模块化单体（Modular Monolith）

为了兼顾大四毕设的开发效率与生产环境的稳定性，项目**放弃分布式微服务架构**，采用**模块化单体架构**。

- **架构优势**：单一 Spring Boot 进程，部署简单，调试方便。
- **内部分层**：代码层面严格按业务领域（用户、订单、钱包、AI、工单）拆分为独立的 Java Module。模块间通过接口交互，严格遵循依赖倒置原则。
- **演进能力**：论文中明确声明，该架构设计为后续向 Spring Cloud 微服务演进提供了清晰的限界上下文划分。

### 4.2 总体分层逻辑（自上而下）

- **表现层（Presentation）**：微信小程序（uni-app / 原生微信小程序框架）+ PC 管理后台（Vue 3 + Element Plus）。
- **接入层（Gateway/Interceptor）**：Spring Security + JWT 实现身份认证与 RBAC 权限拦截，区分 `/api/user`、`/api/worker`、`/api/admin` 路由。
- **业务逻辑层（Business）**：各领域 Service 实现核心业务编排，包含事务管理、状态机驱动、申诉仲裁逻辑。
- **基础设施层（Infrastructure）**：提供 Redis 缓存、RabbitMQ 消息队列、OSS 对象存储、SimpleVectorStore 向量存储等通用能力。

### 4.3 核心技术选型全景表

表格





| 层级         | 技术选型                                 | 说明                                                    |
| :----------- | :--------------------------------------- | :------------------------------------------------------ |
| **后端核心** | Java 17 + Spring Boot 3.x + MyBatis-Plus | 主业务框架                                              |
| **权限安全** | Spring Security + JWT                    | 身份认证与 RBAC                                         |
| **数据存储** | MySQL 8.0                                | 存储用户、订单、钱包、评价、工单等关系型数据            |
| **向量存储** | Spring AI SimpleVectorStore              | 存储 PDF 切片向量，轻量级、无外部依赖，适合毕设数据量级 |
| **缓存**     | Redis                                    | 登录态、会话上下文、热点陪诊师信息、分布式限流计数器    |
| **消息队列** | RabbitMQ                                 | 异步处理 PDF 解析与向量生成任务，削峰填谷               |
| **AI 生态**  | Spring AI 框架                           | 统一封装大模型调用，支持通义千问 / 智谱 AI 的无缝切换   |
| **C 端前端** | 微信小程序                               | 求助者与陪诊师共用，基于微信授权登录与定位              |
| **B 端前端** | Vue 3 + Element Plus                     | PC 端管理后台                                           |

### 4.4 核心数据库表设计概要（逻辑描述）

系统核心围绕以下表构建，采用领域驱动设计（DDD）的贫血模型：

1. **用户与角色表**：包含用户基本信息、微信 OpenID、角色枚举（普通用户 / 陪诊师 / 管理员）及陪诊师扩展审核表。
2. **需求工单表**：记录求助者原始诉求、AI 推荐的医院 JSON 快照及期望服务时间。
3. **订单主表**：系统的**绝对核心**。包含订单状态机字段（整型枚举）、交易金额、关联的陪诊师与用户 ID、GeoHash 位置编码、服务类型预留字段。
4. **钱包账户表**：包含用户总余额、冻结余额。引入 `version` 字段作为**乐观锁**，保证高并发下的余额安全扣减。
5. **资金流水表**：以**幂等键（Txn_No）** 作为唯一索引，记录每一笔充值、消费、收入、退款的变更前后快照，满足金融级审计要求。
6. **知识库文档切片表**：存储 PDF 解析后的纯文本切片，关联其在 SimpleVectorStore 中的向量 ID，记录文档版本与生效状态（待生效 / 已生效 / 已失效）。
7. **评价表**：双向评价设计，包含订单 ID、评价者角色、星级、文本内容、追评/回复字段。
8. **申诉工单表**：包含工单编号、关联订单 ID、申诉人、申诉原因、证据图片 URL、仲裁结果、处理管理员 ID、处理时间。
9. **管理员操作审计表（admin_operation_log）**：记录操作人 ID、操作时间、操作订单号、操作类型（取消订单、强制退款、审核通过/驳回）、变更前后数据快照（JSON）。

------

## 5. 关键技术难点与解决方案（概要设计）

### 5.1 AI 应用落地（RAG 技术实现）

- **难题**：大模型会产生幻觉，不能直接用于医疗咨询。
- **解决策略**：引入 **RAG（检索增强生成）**。平台不依赖大模型自身医学知识，而是提前将公开的《就医指南》、《三甲医院科室介绍》等清洗为结构化文本，通过 Spring AI 的 `SimpleVectorStore` 转为向量存储。用户提问时，先检索向量库召回相关片段，再将"检索到的片段 + 用户问题"拼入 Prompt，强制大模型基于提供资料回答，并附上引用来源。
- **知识库版本控制**：管理后台上传新 PDF 后，通过 RabbitMQ 异步执行解析与向量化。新文档生效后，旧版本批量标记为"已失效"，AI 检索接口仅查询 `status = '已生效'` 的切片，确保回答基于最新资料。

### 5.2 资金账户并发安全（ACID）

- **难题**：下单冻结资金、确认完成时扣款划转，若并发操作可能导致用户余额透支。
- **解决策略**：在钱包表中使用 **乐观锁（Version 字段）** 更新余额。在核心的"确认完成"Service 方法上添加 `@Transactional` 注解，确保"扣求助者钱 + 加陪诊师钱 + 写入两条流水 + 更新订单状态"在同一个数据库事务中执行，要么全部成功，要么全部回滚。
- **压测目标**：单机 200 并发下，QPS 预估 50~80，乐观锁重试 1~2 次即可成功，MySQL 足以应对。

### 5.3 订单履约状态管理

- **难题**：订单有"待接单、已冻结、待服务、服务中、待确认、已完成、已取消"等多种状态，若用简单的 if-else 容易逻辑混乱。
- **解决策略**：引入 **Spring StateMachine（状态机）** 框架。通过定义状态枚举（Enum）和事件枚举（Event），将复杂的业务操作（如：抢单、开始服务、确认完成、超时自动确认）转化为状态驱动的有限状态机流转，极大提高了代码的可维护性和逻辑清晰度。

### 5.4 抢单并发控制

- **难题**：多个陪诊师可能同时抢同一订单。
- **解决策略**：订单表设置 `version` 乐观锁。陪诊师点击抢单时，后端执行 `UPDATE order SET worker_id = ?, status = ?, version = version + 1 WHERE id = ? AND version = ? AND status = '待接单'`。若返回影响行数为 0，则提示"手慢一步，已被其他人抢走"。

### 5.5 扩展性预留（面向未来）

- **设计策略**：在订单主表中显式预留 `service_type` 字段。未来若要接入"上门遛狗"或"家政清洁"服务，只需在策略工厂（Strategy Factory）中添加一个新的业务策略实现类，利用 Java 的多态特性即可无侵入扩展，无需重写核心订单引擎。

------

## 6. 非功能性需求概述

### 6.1 性能与并发

- **响应时间**：核心下单接口需在数据库事务提交后 2 秒内返回前端操作结果。
- **并发预估**：毕设演示使用 JMeter 模拟 200 并发用户，持续压测"下单"和"确认完成"接口。单机 MySQL + 乐观锁场景下，预期 QPS 为 50~80。
- **缓存策略**：Redis 缓存热点陪诊师信息与分布式限流计数器，减少数据库查询压力。**Redis 不用于资金扣减**，资金操作必须通过数据库事务保证一致性。

### 6.2 异步处理

- **文档解析**：PDF 上传采用异步处理，前端上传后立即返回"解析中"状态，通过 RabbitMQ 消费完成后推送微信订阅消息通知管理员。
- **AI 报告生成**：陪诊服务结束后，AI 生成结构化报告采用异步调用，避免阻塞用户确认接口。

### 6.3 安全性

- **用户密码/敏感信息**：微信登录场景下不存储用户密码；涉及敏感操作使用微信手机号授权。
- **JWT Token**：设置合理有效期（如 2 小时），支持刷新机制。
- **接口限流**：基于 Redis 实现分布式限流（如令牌桶/滑动窗口），防止接口被恶意刷取。
- **资金审计**：所有资金变动写入流水表，管理员所有人工干预操作写入 `admin_operation_log`，确保 100% 可追溯。

### 6.4 数据备份与容灾

- **定时备份**：在服务器上配置 Crontab，每天凌晨 2:00 执行 `mysqldump` 全量备份，压缩后上传至阿里云 OSS 或腾讯云 COS。
- **恢复能力**：一旦数据损坏，运维人员可通过 OSS 下载最近一次全量备份恢复，RPO（数据恢复点目标）为 24 小时。

### 6.5 日志与监控

- **业务日志**：订单状态流转、资金变动、管理员操作等关键行为记录结构化日志。
- **异常预警**：服务超时未结束等异常订单自动触发系统标记，管理后台高亮提示。